import { customTheme } from "."

export const dark = {
  ...customTheme,
  title: 'dark',

  colors: {
    background: '#000',
  },
}

export const light = {
  ...customTheme,
  title: 'light',

  colors: {
    background: '#ccc',
  },
}
